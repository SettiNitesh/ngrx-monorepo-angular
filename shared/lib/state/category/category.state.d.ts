import { ICategory } from '../../interface/category';
export interface ICategoryState {
    categories: ICategory[];
}
export declare const initialState: ICategoryState;
export declare const categories = "categories";
